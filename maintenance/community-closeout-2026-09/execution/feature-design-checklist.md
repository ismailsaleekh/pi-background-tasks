## C1/P1/D1/D2 prioritized checklist

1. **Gate first:** extend docs extraction with strict finite feature/shortcut availability; arbitrary conditional registration must still fail. Add `PI_BG_FEATURES` and finite `PI_BG_DOCK_SHORTCUT` parser with loud invalid-input behavior.
2. **Surface split:** preserve full default; process-only has only process commands/tools/UI/EventBus. `bg_result` iff delegate or Fusion. Ambient attribution independent; isolated Anthropic children use a new always-on child attribution entrypoint.
3. **Shortcut:** support `shift+down` (default), `ctrl+alt+b`, `off`; derive footer hint from config; prove `/tasks` fallback, real dispatch, reload, and no conflict with a fixture owning Shift+Down.
4. **Shell:** preserve `$SHELL` default; add `PI_BG_POSIX_SHELL=inherit|bash|sh` plus validated absolute executable path; inject exact executable/dialect guidance; keep Windows argv behavior unchanged; add fake-Nu tests.
5. **Fusion docs:** explain identical parallel candidates, blind evaluator, same-slot optional repair, final merger, stage fan-in/latency/quality tradeoffs, duplicates/`$current`, 5-or-6/zero calls, and subscription-only routes.
6. **Lazy loading:** adapt/credit PR #22 with single-flight `LazyModule`, retained import failure, generation checks, and no post-shutdown side effects. Keep enabled schemas immediate and disabled surfaces absent. Add first-use concurrency/failure/reload tests and packed missing-module test.
7. **Benchmark:** fresh-process real SDK loader, isolated state, ≥30 interleaved samples, raw distribution + provenance for no-extension/process-only/full and first-use paths. No timing CI threshold, Windows claim, compiled-Bun claim, or bundling claim without evidence.
8. **Integrate R1 before D1:** retain pending/delivered/abandoned truth, typed EventBus closure, cleared retry timers, bounded retries, and post-gate closure checks.
9. **D1:** add `bg_run.surviveReload=false` and `/bg --survive-reload`; reject agents/typed tasks. Hand the live child handle/output/timers/cap across only real `reason:"reload"` via a versioned process-global owner and fresh activation lease. No PID/file reattachment. Kill on quit/new/resume/fork and on 30s failed handoff; no crash-survival claim.
10. **D1 proof:** real `AgentSession.reload()` same PID/nonce/task/output, gap completion with real exit, kill/timeout/cap across reload, exactly one logical completion/new-host notification, lifecycle matrix, forged/reused-PID refusal, POSIX and native Windows teardown.
11. **D2 stop boundary:** do not implement with current Pi 0.84/0.86. Host lacks around-tool `next`, effective definition/settings, running execution handle, and cancellation transfer. Reject override/command rewrite/kill-restart approximations.
12. **D2 after host API:** interceptor starts effective Bash once, CAS races terminal/cancel/threshold, promotes spool durably, transfers cancellation, gates notification behind receipt, preserves permission/override/result/timeout/output behavior, and sets promoted jobs `surviveReload:false`.
13. **Integration order:** docs gate → C1 topology/shortcut/shell/docs → P1 → R1 merge → D1 → host API decision → D2. Regenerate docs only at integration; no self-attestation.
