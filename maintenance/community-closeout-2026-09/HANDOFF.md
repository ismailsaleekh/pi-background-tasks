# Paste this after compaction

Continue the autonomous `pi-background-tasks` community closeout. Work from:

`/Users/lizavasilyeva/work/ai-pipeline/packages/pi-background-tasks`

First read the package gateway, `docs/INDEX.md`, `docs/read-before-edit.md`, then all five authority/state documents in `maintenance/community-closeout-2026-09/`: `README.md`, `ACCEPTANCE.md`, `EXECUTION.md`, `BASELINE.md`, `STATE.md`. Verify the frozen evidence hashes. Inspect relevant source, tests, owning docs and frozen issue/PR discussions/diffs until you clearly understand the current behavior and requirements. Do not rely on the old conversation or assume contributor claims are verified.

Then implement and verify the complete frozen queue of 11 issues and 6 PRs, strictly following that process. Start with baseline gate repair and real red reproductions; record product/lifecycle decisions before invasive work. Preserve safety guarantees, compatibility, contributor credit, and opt-in boundaries. Use independent review plus integrated execution evidence, not unit tests or self-certification alone. Continue through the architectural requests, not just the easy patches.

Hard limits: **local package edits and commits only. No push, publish, remote tags, GitHub interaction, parent-repository edits, or monorepo worktrees/copies.** Every auxiliary worktree must come from the standalone background-tasks Git repository and be verified package-only. At most two auxiliary worktrees; follow the disk budget, ownership ledger, and safe cleanup protocol. Integrate/preserve work before removal; remove all task-owned worktrees and disposable large artifacts when finished. Never force-delete dirty/unintegrated or unowned resources.

Use **Sol 5.6 / max thinking via the Codex subscription in Pi**, launched with `bg_run` as an agent, for narrowly scoped workers/reviewers when helpful. Verify the exact route/level; no substitutions, paid frontier APIs, or Fusion maintenance. You are sole integrator. Follow event-driven background completion, not polling.

Do not pause for routine approvals. Escalate only concrete unauthorized/breaking decisions, destructive actions, cost/scope expansion, or inaccessible verification environments. Keep doing independent unblocked work. Missing Windows/Bun/platform evidence must remain an explicit blocker, not a pass or reason to abandon the rest.

Finish with coherent local commits, a per-ticket closure-readiness report linked to source-bound tests, all remaining limitations, final clean package status, and proof that no task-owned workers/worktrees/bulky scratch resources remain. Do not call anything remotely closed, released, or perfectly verified when it is not.
